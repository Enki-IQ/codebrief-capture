---
name: codebrief-logout
description: Log out Codebrief Capture and remove its locally stored CLI credentials.
---

# Codebrief Logout

Resolve `SKILL_DIR` to the absolute directory containing this `SKILL.md`, then run:

```sh
node "$SKILL_DIR/../../scripts/codebrief-cli.js" logout
```
